# 浏览器工作原理

## 1. 浏览器架构

### 多进程架构

现代浏览器采用多进程架构：

```
浏览器进程（Browser Process）→ 负责UI和协调
├── 渲染进程（Renderer Process）→ 负责渲染网页
│   ├── 主线程（Main Thread）→ DOM解析、渲染、JS执行
│   ├── 合成线程（Compositor Thread）→ 图层合成
│   └── 光栅线程（Raster Thread）→ 绘制
├── 网络进程（Network Process）→ 网络请求
├── GPU进程（GPU Process）→ 图形渲染
└── 插件进程（Plugin Process）→ 第三方插件
```

### 渲染进程

- 每个标签页一个渲染进程（沙箱安全）
- 内部是多线程协作

## 2. 渲染流程

### 1. 解析HTML → DOM树

```html
<div class="container">
  <p>Hello World</p>
</div>
```
→ DOM树
- `#document`
  - `div.container`
    - `p`
      - Text: "Hello World"

### 2. 解析CSS → CSSOM树

```css
.container {
  display: flex;
}

p {
  color: red;
  font-size: 18px;
}
```
→ CSSOM树

### 3. DOM + CSSOM = 渲染树（Render Tree）

只包含可见元素，隐藏的`display: none`不会出现在渲染树上。

### 4. 布局（Layout）

计算每个节点的尺寸和位置，生成**布局树（Layout Tree）**。

### 5. 绘制（Painting）

将每个节点绘制成图层（Layer），生成**绘制列表（Paint Record）**。

### 6. 合成（Compositing）

将图层合成为页面，发送给GPU显示。

## 3. 关键渲染路径

### 优化建议

1. **减少HTML/CSS体积**：压缩、删除注释
2. **优化CSS选择器**：避免复杂选择器
3. **JS优化**：
   - 避免DOM操作
   - 防抖节流
   - 减少重排重绘
4. **图片优化**：懒加载、压缩
5. **字体优化**：预加载、使用系统字体

## 4. 渲染阻塞

- **CSS阻塞**：外部CSS会阻塞渲染
  - 优化：媒体查询、异步加载
- **JS阻塞**：JS执行会阻塞DOM解析
  - 优化：`defer`、`async`属性

## 5. 事件循环（Event Loop）

### 执行栈（Call Stack）

```javascript
function foo() {
  bar();
}

function bar() {
  // 执行
}

foo();
```

### 消息队列（Message Queue）

任务队列：宏任务（Macro Task）→ 微任务（Micro Task）

### 执行顺序

```javascript
// 宏任务1 → 所有微任务 → 宏任务2...
console.log('1');

setTimeout(() => console.log('2'), 0); // 宏任务

Promise.resolve().then(() => console.log('3')); // 微任务

console.log('4');
```

输出：`1 → 4 → 3 → 2`

## 6. V8引擎

### 垃圾回收（GC）

- 新生代（Minor GC）：小对象，采用Scavenge算法
- 老生代（Major GC）：大对象，采用Mark-Sweep-Compact算法

### 内存泄漏

常见原因：
- 未清除的事件监听
- 闭包引用
- 定时器未清除
- 全局变量过多

## 7. 浏览器存储

### Cookie

- 4KB限制，每次请求携带
- 会话Cookie、持久Cookie

### LocalStorage/SessionStorage

- LocalStorage：持久化，5MB左右
- SessionStorage：会话期间，关闭标签页清除

### IndexedDB

- 本地数据库，支持复杂查询，存储空间大（通常50MB或更多）

## 8. 跨域与同源策略

### 同源策略

协议、域名、端口都相同才是同源。

### 跨域解决方案

```javascript
// 1. CORS（服务端设置）
// Access-Control-Allow-Origin: *

// 2. JSONP（动态script标签）
const script = document.createElement('script');
script.src = 'http://api.example.com?callback=handleData';
document.body.appendChild(script);

// 3. 代理（Proxy）
// Nginx代理、开发服务器代理
```

## 总结

浏览器原理是前端性能优化的基础，理解这些原理能帮助我们更好地优化网页加载和渲染。
